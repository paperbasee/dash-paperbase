/**
 * Settings URL (without locale) that opens the Shipping section.
 *
 * Kept free of imports: the legacy /shipping server redirect uses it.
 */
export const SHIPPING_SETTINGS_HREF = "/settings?tab=shipping";
