/**
 * Primary image (Product.image) + gallery uploads (ProductImage) must not exceed this total.
 * Keep in sync with backend `engine.apps.products.constants.MAX_PRODUCT_IMAGES_TOTAL`.
 */
export const MAX_PRODUCT_IMAGES = 5;
